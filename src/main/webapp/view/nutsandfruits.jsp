
<table class="childTable">
	<tr>
		<th>
			<a href="">Nuts & Dry Fruits
			<br/>
			<c:if test="${sessionScope.locale != null && sessionScope.locale != 'en'}">
				<spring:message code="label.toorDal"/>
			</c:if>
			</a>
		</th>
	</tr>															
	<tr>
		<td>
			<a href="">Almonds
			<c:if test="${sessionScope.locale != null && sessionScope.locale != 'en'}">
				<spring:message code="label.toorDal"/>
			</c:if>
			</a>
		</td>
	</tr>
	<tr>
		<td>
			<a href="">Cashew
			<c:if test="${sessionScope.locale != null && sessionScope.locale != 'en'}">
				<spring:message code="label.toorDal"/>
			</c:if>
			</a>
		</td>
	</tr>
	<tr>
		<td>
			<a href="">Raisins
			<c:if test="${sessionScope.locale != null && sessionScope.locale != 'en'}">
				<spring:message code="label.toorDal"/>
			</c:if>
			</a>
		</td>
	</tr>
	<tr>
		<td>
			<a href="">Pistachio
			<c:if test="${sessionScope.locale != null && sessionScope.locale != 'en'}">
				<spring:message code="label.toorDal"/>
			</c:if>
			</a>
		</td>
	</tr>																				
	<tr>
		<td>
			<a href="">Peanuts
			<c:if test="${sessionScope.locale != null && sessionScope.locale != 'en'}">
				<spring:message code="label.toorDal"/>
			</c:if>
			</a>			
		</td>
	</tr>																				
	<tr>
		<td>
			<a href="">Coconut
			<c:if test="${sessionScope.locale != null && sessionScope.locale != 'en'}">
				<spring:message code="label.toorDal"/>
			</c:if>
			</a>
		</td>
	</tr>																				
</table>

